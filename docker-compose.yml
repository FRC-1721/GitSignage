version: '3.7'

services:
  gitsignage:
    image: gitsignage:$TAG
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      GITHUB_TOKEN: "top.secret.token"
    restart: "no"
    command: "true"
